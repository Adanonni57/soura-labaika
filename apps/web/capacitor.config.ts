import type {CapacitorConfig} from '@capacitor/cli';
const config:CapacitorConfig={appId:'bf.labaika.soura',appName:'Labaïka SOURA',webDir:'dist',server:{androidScheme:'https'}};
export default config;
